import * as tslib_1 from "tslib";
import { Directive, ChangeDetectorRef } from '@angular/core';
import { GridComponent } from '../grid.component';
import { Selection } from "./selection-default";
/**
 * A directive which stores the row selection state of the Grid in memory.
 */
let SelectionDirective = class SelectionDirective extends Selection {
    constructor(grid, cd) {
        super(grid, cd);
        this.grid = grid;
    }
    /**
     * @hidden
     */
    ngOnInit() {
        if (this.grid.selectable === false) {
            this.grid.selectable = true;
        }
        this.grid.selectionDirective = true;
    }
    /**
     * @hidden
     */
    ngOnDestroy() {
        super.destroy();
    }
};
SelectionDirective = tslib_1.__decorate([
    Directive({
        selector: '[kendoGridSelectBy]'
    }),
    tslib_1.__metadata("design:paramtypes", [GridComponent, ChangeDetectorRef])
], SelectionDirective);
export { SelectionDirective };
