/**
 * Sets the sort settings of the column.
 */
export declare type ColumnSortSettings = boolean | {
    /**
     * Enables the removal of the column sorting.
     */
    allowUnsort?: boolean;
    /**
     * Determines the initial (from un-sorted to sorted state) sort direction. The supported values are:
     * - `asc` (default)
     * - `desc`
     */
    initialDirection?: 'asc' | 'desc';
};
/**
 * Sets the sort settings of the Grid.
 */
export declare type SortSettings = boolean | ColumnSortSettings & {
    /**
     * The sort mode of the Grid. The available modes for sorting are:
     * - `single`
     * - `multiple`
     */
    mode?: 'single' | 'multiple';
    /**
     * Enables sort sequence indicators for multiple columns sorting.
     */
    showIndexes?: boolean;
};
/**
 * @hidden
 */
export declare const normalize: (...settings: (boolean | {
    allowUnsort?: boolean;
    initialDirection?: "desc" | "asc";
} | (true & {
    mode?: "single" | "multiple";
    showIndexes?: boolean;
}) | (false & {
    mode?: "single" | "multiple";
    showIndexes?: boolean;
}) | ({
    allowUnsort?: boolean;
    initialDirection?: "desc" | "asc";
} & {
    mode?: "single" | "multiple";
    showIndexes?: boolean;
}))[]) => any;
